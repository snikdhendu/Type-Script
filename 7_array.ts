const age : number []   =  []

age.push(10)

// we can write array in some differnet ways let's see how

const agegap: Array <number> =  []
agegap.push(20,30)

type User={
    name:string,
    isActive:boolean
}

// now we can also declare a array by using User data type

const allUsers: User[]=[]

allUsers.push({ name:"Snikdhe", isActive:true })

// array inside an array
const MLModels:number [][]=[
    [23,45,44],
    [34,56,67]

]


